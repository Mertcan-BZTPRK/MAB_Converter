<Window x:Class="MAB_Converter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MAB Converter" Height="600" Width="850"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Icon="IMGS/iconbig.png">

    <Window.Resources>
        <SolidColorBrush x:Key="BgColor" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="PanelColor" Color="#2D2D30"/>
        <SolidColorBrush x:Key="TextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentColor" Color="#4CAF50"/>
        <SolidColorBrush x:Key="TitleBarColor" Color="#151515"/>

        <Style x:Key="GlassButton" TargetType="Button">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0FFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="8" Padding="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBarThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{DynamicResource AccentColor}" CornerRadius="4" Margin="2,0,2,0" Opacity="0.8"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1" Value="{TemplateBinding VerticalOffset}" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Width="8" AutomationProperties.AutomationId="VerticalScrollBar">
                                <ScrollBar.Template>
                                    <ControlTemplate TargetType="ScrollBar">
                                        <Grid x:Name="Bg" Background="Transparent" SnapsToDevicePixels="true">
                                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                                <Track.Thumb>
                                                    <Thumb Style="{StaticResource ModernScrollBarThumb}" />
                                                </Track.Thumb>
                                            </Track>
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollBar.Template>
                            </ScrollBar>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" CornerRadius="6" Margin="4,2">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ModernComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="0">
                                <Grid>
                                    <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" Cursor="Hand">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <Path x:Name="Arrow" Fill="{DynamicResource TextColor}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0" Data="M 0 0 L 5 5 L 10 0 Z"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="15,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="{DynamicResource PanelColor}" BorderBrush="{DynamicResource AccentColor}" BorderThickness="1.5" CornerRadius="8" Margin="0,5,0,0" MinWidth="{Binding ActualWidth, ElementName=border}">
                                    <ScrollViewer Margin="0,5,0,5" SnapsToDevicePixels="True" Style="{StaticResource ModernScrollViewer}">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource BgColor}" CornerRadius="15" BorderBrush="{DynamicResource AccentColor}" BorderThickness="1.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource TitleBarColor}" CornerRadius="13.5,13.5,0,0" MouseDown="TitleBar_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <Border Background="#4CAF50" Width="8" Height="25" CornerRadius="2" Margin="0,0,10,0"/>
                        <TextBlock Text="MAB TECH" FontWeight="Black" FontSize="18" Foreground="#4CAF50" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonDown="Logo_Click" ToolTip="Geliştirici Profiline Git (LinkedIn)"/>
                        <TextBlock Text=" | CONVERTER" FontWeight="Regular" FontSize="18" Foreground="{DynamicResource TextColor}" Opacity="0.7" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                        <Button x:Name="btnThemeToggle" Content="☀️ Light Mod" Click="BtnThemeToggle_Click" Width="90" Margin="0,8,15,8" Style="{StaticResource GlassButton}" BorderThickness="0"/>
                        <Button x:Name="btnMinimize" Content="—" Click="BtnMinimize_Click" Width="35" Margin="0,10,5,10" Style="{StaticResource GlassButton}" FontWeight="Bold"/>
                        <Button x:Name="btnMaximize" Content="⬜" Click="BtnMaximize_Click" Width="35" Margin="0,10,5,10" Style="{StaticResource GlassButton}"/>
                        <Button x:Name="btnClose" Content="✕" Click="BtnClose_Click" Width="35" Margin="0,10,0,10" Style="{StaticResource GlassButton}" Foreground="#FF5252" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource PanelColor}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Video Linki (YouTube, Instagram, X vb.):" Foreground="{DynamicResource TextColor}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="txtUrl" Grid.Column="0" Height="35" VerticalContentAlignment="Center" Background="#3E3E42" Foreground="White" Margin="0,0,15,0" Style="{StaticResource RoundedTextBox}"/>
                            <Button x:Name="btnAnalyze" Grid.Column="1" Click="btnAnalyze_Click" Content="Analiz Et" Width="130" Height="35" Background="{DynamicResource AccentColor}" Foreground="White" FontWeight="Bold" Style="{StaticResource GlassButton}"/>
                        </Grid>

                        <Grid x:Name="gridVideoInfo" Visibility="Collapsed" Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border CornerRadius="8" ClipToBounds="True" Width="120" Height="67" BorderThickness="1" BorderBrush="#33FFFFFF">
                                <Image x:Name="imgThumbnail" Stretch="UniformToFill"/>
                            </Border>
                            <TextBlock x:Name="lblVideoTitle" Grid.Column="1" Text="Video Başlığı" TextWrapping="Wrap" Foreground="{DynamicResource TextColor}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="15,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{DynamicResource PanelColor}" CornerRadius="12" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Format ve Kalite:" Foreground="{DynamicResource TextColor}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <Border CornerRadius="8" Background="#3E3E42" Height="35">
                                <ComboBox x:Name="cmbFormat" Style="{StaticResource ModernComboBox}" Background="#3E3E42" Margin="0,2,0,0">
                                    <ComboBoxItem Content="MP4 - Maksimum Kalite" IsSelected="True"/>
                                </ComboBox>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="{DynamicResource PanelColor}" CornerRadius="12" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Kırpma Ayarları (Dk : Sn):" Foreground="{DynamicResource TextColor}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Başlangıç:" Foreground="{DynamicResource TextColor}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBox x:Name="txtStartMin" Width="35" Height="30" Text="00" TextAlignment="Center" VerticalContentAlignment="Center" Background="#3E3E42" Foreground="White" Style="{StaticResource RoundedTextBox}"/>
                                <TextBlock Text=":" Foreground="{DynamicResource TextColor}" VerticalAlignment="Center" Margin="5,0,5,0" FontWeight="Bold"/>
                                <TextBox x:Name="txtStartSec" Width="35" Height="30" Text="00" TextAlignment="Center" VerticalContentAlignment="Center" Background="#3E3E42" Foreground="White" Style="{StaticResource RoundedTextBox}"/>

                                <TextBlock Text="Bitiş:" Foreground="{DynamicResource TextColor}" VerticalAlignment="Center" Margin="20,0,10,0"/>
                                <TextBox x:Name="txtEndMin" Width="35" Height="30" Text="00" TextAlignment="Center" VerticalContentAlignment="Center" Background="#3E3E42" Foreground="White" Style="{StaticResource RoundedTextBox}"/>
                                <TextBlock Text=":" Foreground="{DynamicResource TextColor}" VerticalAlignment="Center" Margin="5,0,5,0" FontWeight="Bold"/>
                                <TextBox x:Name="txtEndSec" Width="35" Height="30" Text="00" TextAlignment="Center" VerticalContentAlignment="Center" Background="#3E3E42" Foreground="White" Style="{StaticResource RoundedTextBox}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="2" Background="{DynamicResource PanelColor}" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <Button x:Name="btnDownload" Click="btnDownload_Click" Content="VİDEOYU İNDİR" Height="50" FontWeight="Bold" FontSize="16" Background="{DynamicResource AccentColor}" Style="{StaticResource GlassButton}" IsEnabled="False"/>
                        <Border CornerRadius="5" ClipToBounds="True" Height="12" Margin="0,20,0,0">
                            <ProgressBar x:Name="pbDownload" Maximum="100" BorderThickness="0" Background="#3E3E42" Foreground="{DynamicResource AccentColor}"/>
                        </Border>
                        <TextBlock x:Name="lblStatus" Text="Sistem Hazır. Bağlantı bekleniyor..." Foreground="{DynamicResource TextColor}" HorizontalAlignment="Center" Margin="0,15,0,0" FontWeight="SemiBold" Opacity="0.7"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>